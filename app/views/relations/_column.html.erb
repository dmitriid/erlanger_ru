<%
   authors = material.author
   urls = material.url
   events = material.event
   news = material.news
   articles = material.article
%>
<% unless authors.empty? %>
  <h3><%=t('relation.authors')%></h3>
  <% authors.each do |ref| %>
    <%= link_to ref.name, ref %><br/>
  <% end %>
<% end %>

<% unless urls.empty? %>
  <h3><%= t('relation.links') %></h3>
  <% urls.each do |ref| %>
    <%= link_to ref.name, ref.url %><br/>
  <% end %>
<% end %>

<% unless events.empty? %>
  <h3><%= t('relation.events') %></h3>
  <% events.each do |ref| %>
    <%= link_to ref.title, ref %><br/>
  <% end %>
<% end %>

<% unless news.empty? %>
  <h3><%= t('relation.news') %></h3>
  <% news.each do |ref| %>
    <%= link_to ref.title, ref %><br/>
  <% end %>
<% end %>

<% unless articles.empty? %>
  <h3><%= t('relation.articles') %></h3>
  <% articles.each do |ref| %>
    <%= link_to ref.title, ref %><br/>
  <% end %>
<% end %>
